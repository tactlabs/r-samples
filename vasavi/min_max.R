 x = c(1,2,5,3,7,5)
 x
min(x)   #minimum value

max(x)     #maximum value

range(x)    #range 


which.min(x)    #find index of the minimum number

which.max(x)    #find index of the maximum number

x[which.min(x)]    #alternative way to find the minimum
 
