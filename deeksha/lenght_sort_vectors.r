#lenght
fruits <- c("banana", "apple", "orange")

length(fruits)

# sort
fruits <- c("banana", "apple", "orange", "mango", "lemon")
num <- c(13, 3, 5, 7, 20, 2)

sort(fruits) 
# Sort a string by alphabetical order
sort(num)
# Sort numbers by numberic order